---
phase: 08.2-admin-triage-and-ban-workflow
plan: 08.2-01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/accounts/models.py
  - apps/accounts/migrations/0002_user_banned_until.py
  - apps/accounts/serializers.py
  - apps/moderation/services.py
  - apps/moderation/tasks.py
  - apps/moderation/serializers.py
  - apps/moderation/views.py
  - apps/moderation/urls.py
  - apps/moderation/admin.py
  - apps/community/models.py
  - apps/community/migrations/0002_announcement.py
  - apps/community/admin.py
  - apps/community/serializers.py
  - apps/community/views.py
  - apps/community/urls.py
  - apps/notifications/tasks.py
  - config/celery.py
  - config/settings/base.py
  - apps/moderation/tests/test_ban_workflow.py
  - apps/moderation/tests/test_review_api.py
  - apps/moderation/tests/test_admin_actions.py
  - apps/accounts/tests/test_login_suspension.py
  - apps/community/tests/test_announcements_api.py
  - apps/community/tests/test_announcement_broadcast.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [MOD-05, MOD-06]
user_setup: []
must_haves:
  truths:
    - "D1 (CONTEXT): banning runs 08 §6's two steps — one transaction commits `is_active=False` plus the §11.1 MODERATION_ACTION_TAKEN log line, then a Celery task (`moderation.tasks.sever_banned_user_sessions`, default queue) bulk-blacklists every outstanding refresh token via `accounts.services.revoke_user_refresh_tokens` and deactivates all `Device` rows. The REST/Admin caller returns 202 Accepted. The task is idempotent and re-runnable; re-banning re-fires it."
    - "D2 (CONTEXT): unban reactivates `is_active` (clearing `banned_until`) but NEVER reactivates devices or tokens — re-registration is the sanctioned path."
    - "D3 (CONTEXT): temporary suspensions ship — `User.banned_until` (nullable), `duration_days` on the ban request (0/absent = permanent), and `moderation.tasks.auto_reinstate_users` on a NEW hourly beat entry (maintenance queue)."
    - "D4 (CONTEXT): the review endpoint executes five actions (T8.7/MOD-05): DISMISS → status DISMISSED; REMOVE_CONTENT, LOCK_POST, WARN_USER, BAN_USER → RESOLVED; every action writes `reviewed_by`, `reviewed_at`, `moderator_notes` (never serialized to candidates)."
    - "D5 (CONTEXT): BAN_USER-from-review and POST /moderation/users/{id}/ban/ share one `services.ban_user`; the review path bans the report's content author; LOCK_POST is rejected 400 on comment-targeted reports."
    - "D6 (CONTEXT): the queue orders by 08 §4.1's static REPORT_SEVERITY_WEIGHTS (SCAM 100 → OTHER 10) then -created_at; the log2 velocity multiplier is a recorded divergence."
    - "D7 (CONTEXT): announcements ship at full T8.6+T8.10 scope — `community.Announcement` per 08 §7.1, staff CRUD + public read (04 §71–§75), and a broadcast task implemented at the byte-exact reserved name `notifications.tasks.broadcast_announcement` (route already in CELERY_TASK_ROUTES). In-app ANNOUNCEMENT rows are created for every active user unconditionally; the push is preference-gated by the existing send_push_notification checks."
    - "D9 (CONTEXT): the audit trail is structured logging only — every triage/ban action emits one §11.1-shaped JSON event on the `moderation` logger; no AuditLog model exists."
    - "§6.2: a banned user's login attempt returns 403 `ACCOUNT_SUSPENDED`; a request with an unexpired access token returns 401 (`USER_INACTIVE`) — verified, and the login check applies only after successful credential validation (anti-enumeration preserved)."
    - "Byte-exact reserved names: `community.tasks.clean_expired_announcements` (beat, hourly :15), `notifications.tasks.broadcast_announcement` (route, notifications queue), `moderation.tasks.auto_reinstate_users` (new beat + route, maintenance queue) — the 7.2 route-mismatch lesson."
  artifacts:
    - path: "apps/accounts/migrations/0002_user_banned_until.py"
      provides: "`User.banned_until` nullable DateTimeField (08 §6.1 temporary suspensions)"
      contains: "banned_until"
    - path: "apps/moderation/services.py"
      provides: "ban_user / unban_user / dismiss_report / resolve_report_by_action — the single write gate shared by REST and Admin"
      contains: "def ban_user"
    - path: "apps/moderation/tasks.py"
      provides: "`sever_banned_user_sessions` (token blacklist + device deactivation, idempotent) and `auto_reinstate_users`"
      contains: "sever_banned_user_sessions"
    - path: "apps/moderation/views.py"
      provides: "GET /api/v1/moderation/reports/ (§68), POST .../review/ (§69), POST /moderation/users/{id}/ban|unban/ (§70) — staff-only"
      contains: "ReportReviewView"
    - path: "apps/moderation/admin.py"
      provides: "ReportAdmin with §9.1 filters/search/readonly + bulk actions (dismiss, soft-delete+resolve, lock, warn, ban) and a staff-safe User ban/unban action"
      contains: "class ReportAdmin"
    - path: "apps/community/models.py"
      provides: "`Announcement` per 08 §7.1 (draft → published → pinned → expires) with ordering + indexes"
      contains: "class Announcement"
    - path: "apps/community/views.py"
      provides: "Announcement REST: public GET (published, non-expired, pinned-first) + staff POST/PATCH/DELETE (04 §71–§75)"
      contains: "AnnouncementListCreateView"
    - path: "apps/notifications/tasks.py"
      provides: "broadcast_announcement at the reserved route name — in-app rows for all active users + chunked push fan-out"
      contains: "def broadcast_announcement"
    - path: "config/celery.py"
      provides: "beat entries: auto-reinstate (new, maintenance) + the already-reserved clean-expired-announcements left byte-identical"
      contains: "moderation.tasks.auto_reinstate_users"
    - path: "config/settings/base.py"
      provides: "MODERATION_TEMP_BAN_DAYS = 7, ANNOUNCEMENT_PUSH_CHUNK = 500, route entries for the two new tasks"
      contains: "MODERATION_TEMP_BAN_DAYS"
  key_links:
    - from: "apps/moderation/tasks.py"
      to: "apps/accounts/services.py"
      via: "sever task calls revoke_user_refresh_tokens verbatim (2.2 extracted it for MOD-06)"
      pattern: "revoke_user_refresh_tokens"
    - from: "apps/moderation/views.py"
      to: "apps/moderation/services.py"
      via: "review/ban/unban views are thin translators over the same services the Admin actions call"
      pattern: "services\\.ban_user"
    - from: "apps/moderation/admin.py"
      to: "apps/moderation/services.py"
      via: "§9.1 bulk actions delegate to services (no independent update() shortcuts for resolve/ban)"
      pattern: "resolve_report_by_action|ban_user"
    - from: "apps/notifications/tasks.py"
      to: "apps/community/models.py"
      via: "broadcast_announcement reads Announcement and fans out rows + pushes; name byte-matches CELERY_TASK_ROUTES"
      pattern: "name=\"notifications.tasks.broadcast_announcement\""
    - from: "config/celery.py"
      to: "apps/community/tasks.py|apps/moderation/tasks.py"
      via: "beat entry task names byte-match decorator names or routes silently do not apply"
      pattern: "clean_expired_announcements|auto_reinstate_users"
---

<objective>
Deliver MOD-05 and MOD-06 (roadmap plan 08-03, MVP tasks T8.6–T8.10): staff report triage over
REST and Django Admin with the five moderation actions, the ban protocol per 08 §6 (transaction +
Celery severing, temporary + permanent), §11.1 structured moderation logging, and the announcement
system deferred here by 6.2 D15 — model, REST CRUD, FCM broadcast, and the beat-reserved expiry task.

User decisions binding this plan (CONTEXT.md): D1/D2 (spec-literal transaction + Celery split;
idempotent severing; unban never revives devices), D3 (temporary bans with auto-reinstatement),
D4/D5 (five actions; shared ban service), D6 (static severity ordering), D7 (full announcement
scope, in-app rows unconditional, push preference-gated), D9 (structured logging only — no
AuditLog model). Explicitly out of scope: the F1/deletion-hook fix in `anonymize_delete_account`
(user chose a separate fix), admin-editable scam patterns, §10 day-90 minimization.

Execution note (08.1-01 issue): pytest lives in the persistent `web` service — run tests with
`docker compose exec -T web`, not `docker compose run --rm web`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/TCS-JL-08.2-admin-triage-and-ban-workflow/8.2-CONTEXT.md
@08_MODERATION.md
@04_API_SPECIFICATION.md
@apps/moderation/models.py
@apps/moderation/services.py
@apps/moderation/views.py
@apps/accounts/services.py
@apps/accounts/views.py
@apps/notifications/tasks.py
@apps/notifications/services.py
@apps/community/urls.py
@config/celery.py
@config/settings/base.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Ban foundations — `banned_until`, ban/unban/reinstate services, suspension login, structured logging</name>
  <files>apps/accounts/models.py, apps/accounts/migrations/0002_user_banned_until.py, apps/accounts/serializers.py, apps/moderation/services.py, apps/moderation/tasks.py, config/settings/base.py, apps/moderation/tests/test_ban_workflow.py, apps/accounts/tests/test_login_suspension.py</files>
  <read_first>
    - 08_MODERATION.md §6 (ban timeline), §6.1 (temporary/permanent table), §6.2 (banned-user responses), §11.1 (log event shape)
    - .planning/phases/TCS-JL-08.2-admin-triage-and-ban-workflow/8.2-CONTEXT.md (D1/D2/D3/D9)
    - apps/accounts/services.py (`revoke_user_refresh_tokens` — reuse verbatim), apps/accounts/serializers.py (FamilyTokenObtainPairSerializer.validate)
    - apps/notifications/models.py (Device.is_active), config/settings/base.py (CELERY_TASK_ROUTES shape)
  </read_first>
  <action>
    1. `apps/accounts/models.py`: add `banned_until = models.DateTimeField(null=True, blank=True,
       help_text="Temporary suspension boundary (08 §6.1); NULL while banned = permanent")` beside
       `is_active`. Generate migration `0002_user_banned_until` and apply it.
    2. `config/settings/base.py`: add `MODERATION_TEMP_BAN_DAYS = 7` (08 §6.1's temporary level;
       read at call time) and route entries `"moderation.tasks.sever_banned_user_sessions":
       {"queue": "default"}` and `"moderation.tasks.auto_reinstate_users": {"queue": "maintenance"}`.
    3. `apps/moderation/services.py` (append; do not touch create_report):
       - `log_moderation_action(**fields)` — emits one `logger.info("MODERATION_ACTION_TAKEN ...")`
         JSON event on the `moderation` logger with the §11.1 keys (timestamp, moderator_id,
         report_id/target_id when present, action, reason, notes). The single logging helper (D9).
       - `ban_user(moderator, user, *, reason, duration_days=0, report=None)` — the ONLY ban writer
         (D5). In ONE `transaction.atomic()`: refuse if `user.is_staff` or `user == moderator`
         (`ModerationTargetError` → 400 envelope); set `is_active=False`; set `banned_until` to
         `now + MODERATION_TEMP_BAN_DAYS` when `duration_days` > 0 else `None`; emit the §11.1 log
         line (D9 — this is the "AuditLog entry" of §6 step 1). After the transaction commits,
         dispatch `sever_banned_user_sessions.delay(str(user.id))`. Returns the user. Idempotent:
         re-banning an already-banned user re-fires the sever task (D1/D2).
       - `unban_user(moderator, user)` — sets `is_active=True`, `banned_until=None`, logs the
         action; does NOT touch devices or token blacklists (D2).
       - `resolve_report(report, moderator, *, action, moderator_notes="", duration_days=0)` —
         validates the action against the five (D4); writes `reviewed_by`/`reviewed_at`/
         `moderator_notes`; sets status DISMISSED for DISMISS, RESOLVED otherwise; executes the
         action's effect: REMOVE_CONTENT soft-deletes the target post/comment
         (`is_deleted=True`, save with `update_fields`) and creates the author's MODERATION
         notification per §4.2.2's message; LOCK_POST (post-targeted reports only — raise
         `InvalidReportTarget` on comments) sets `post.is_locked=True`; WARN_USER creates the
         author's MODERATION warning notification; BAN_USER calls `ban_user` on the content
         author. Emits the log line with report_id populated. Idempotent on already-tombstoned
         targets (re-flag + resolve, 8.1's tombstone-inclusive evidence rule).
    4. `apps/moderation/tasks.py`: `sever_banned_user_sessions(user_id)` — idempotently call
       `revoke_user_refresh_tokens(User.objects.get(id=user_id))` then
       `Device.objects.filter(user_id=user_id, is_active=True).update(is_active=False,
       updated_at=timezone.now())`; log both counts. Swallow DoesNotExist (user deleted
       mid-flight). `auto_reinstate_users()` — reinstate users whose `banned_until <= now`:
       `is_active=True`, `banned_until=None`, one log line per user + a summary count; returns
       the count.
    5. `apps/accounts/serializers.py` (FamilyTokenObtainPairSerializer.validate): after
       successful credential validation, if the authenticated user has `is_active=False`, raise
       an authentication failure the accounts exception handler renders as 403 with code
       `ACCOUNT_SUSPENDED` and §6.2's message. The check sits AFTER the password check so
       anti-enumeration is preserved (wrong password on a banned account still gets the generic
       invalid-credentials response).
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c "python -m pytest apps/moderation/tests/test_ban_workflow.py apps/accounts/tests/test_login_suspension.py -q"</automated>
    <fails_when>Ban leaves tokens unblacklisted or devices active after the sever task runs; unban reactivates devices; a temporary ban never auto-reinstates; banned login returns 200 or generic-invalid-credentials; a staff member can be banned.</fails_when>
  </verify>
  <acceptance_criteria>
    - ban_user → user.is_active=False (+ banned_until set iff duration_days>0), one §11.1 log
      line, and after running the sever task: every OutstandingToken blacklisted, all
      Devices.is_active=False.
    - The sever task is idempotent — running it twice changes nothing the second time.
    - unban_user → is_active=True, banned_until=None, devices remain inactive (D2).
    - auto_reinstate_users flips only expired temporary bans; permanent bans are untouched.
    - Banned user login → 403 `ACCOUNT_SUSPENDED`; wrong password for a banned user → generic
      invalid credentials (anti-enumeration); access-token request with inactive user → 401.
    - ban_user on a staff member or self → refused with a typed error, no state change.
  </acceptance_criteria>
  <done>The ban protocol's transaction half, the sever/reinstate tasks, and the suspended-login contract all exist and are proven.</done>
</task>

<task type="auto">
  <name>Task 2: Triage REST surface — queue list, review action, ban/unban endpoints</name>
  <files>apps/moderation/serializers.py, apps/moderation/views.py, apps/moderation/urls.py, apps/moderation/tests/test_review_api.py</files>
  <read_first>
    - 04_API_SPECIFICATION.md §67–§70 (endpoint shapes; response bodies; permission mandates)
    - 08_MODERATION.md §4.1 (severity weights), §4.2.1–§4.2.5 (action effects), §9.2
    - apps/moderation/views.py (8.1's typed-exception + envelope style; throttle_scope-on-view note)
    - apps/community/permissions.py (IsModerator)
  </read_first>
  <action>
    1. `apps/moderation/serializers.py` (append):
       - `ModerationReportSerializer` — §68's list shape exactly: id, reason, status, post_id,
         comment_id, created_at (+ `description` truncated for triage context). Deliberately
         EXCLUDES reporter email, reviewed_by, moderator_notes (04 §63, §14.5 — no moderation
         metadata to ordinary users; this serializer is staff-facing but the exclusion keeps the
         envelope leak-proof if ever reused).
       - `ReportReviewSerializer` — `action` (ChoiceField: DISMISS/REMOVE_CONTENT/LOCK_POST/
         WARN_USER/BAN_USER), `moderator_notes` (optional, ≤1000), `duration_days` (optional
         IntegerField ≥0, default 0, used only by BAN_USER).
    2. `apps/moderation/views.py` (append):
       - `_ModerationError` (400) and `_NotFound`-style typed exceptions in the 8.1 envelope style.
       - `ModerationReportListView(APIView)` — GET, permissions [IsAuthenticated, IsActive,
         IsVerified, IsModerator]; `?status=` whitelist-validated (PENDING default; unknown → 400
         `invalid_filter`, 7.2's filter-validation precedent); ordering: severity weight
         (`REPORT_SEVERITY_WEIGHTS` module constant per §4.1) then -created_at (D6); paginated
         with the global envelope; select_related post/comment (no N+1).
       - `ReportReviewView(APIView)` — POST `/moderation/reports/{id}/review/`; same permission
         stack; translates service errors: unknown report → 404 `report_not_found`;
         InvalidReportTarget (LOCK_POST on a comment) → 400; ModerationTargetError → 400.
         Success → 200 with §69's shape `{id, status, action}` for synchronous actions (DISMISS,
         REMOVE_CONTENT, LOCK_POST, WARN_USER), and 202 with `{id, status, action,
         detail: "enforcement scheduled"}` when the action dispatched the sever task
         (BAN_USER — D1's async consequence).
       - `UserBanView` / `UserUnbanView` — POST `/moderation/users/{id}/ban|unban/`; body
         `{reason, duration_days?}`; same permission stack; 404 user_not_found for unknown ids;
         202 on ban (sever task dispatched), 200 on unban. Response carries only id/is_active/
         banned_until — never email or profile fields (04 §70 "avoid exposing private account
         data").
    3. `apps/moderation/urls.py`: add the three routes under the existing `/api/v1/` include —
       `moderation/reports/`, `moderation/reports/<str:report_id>/review/`,
       `moderation/users/<str:user_id>/ban|unban/`. Use `<str:pk>` converters (community's F3
       lesson: the uuid converter 404s outside the project envelope; clean invalid UUIDs into
       404s in the views).
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c "python -m pytest apps/moderation/tests/test_review_api.py -q"</automated>
    <fails_when>A non-staff user reaches any moderation endpoint (must be 403); the list leaks moderator_notes/reporter email; LOCK_POST on a comment-targeted report succeeds; DISMISS marks RESOLVED; BAN_USER never resolves the report or never dispatches the sever task; unknown status filter value is not 400.</fails_when>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/moderation/reports/?status=PENDING → §68 shape, ordered SCAM first then
      newest-first; ?status=BOGUS → 400 invalid_filter.
    - Candidate (non-staff) on any of the three endpoints → 403.
    - DISMISS → 200 {status: DISMISSED}; REMOVE_CONTENT → post tombstoned + author notified +
      200 {status: RESOLVED}; WARN_USER → author notification, content untouched; LOCK_POST →
      post.is_locked=True (and 400 on a comment-targeted report).
    - BAN_USER → report RESOLVED + 202; author is_active=False; after the sever task: sessions
      and devices dead.
    - POST /moderation/users/{id}/ban/ with duration_days=7 → 202 + banned_until ≈ now+7d;
      unban → 200 + reactivated; responses contain no email.
  </acceptance_criteria>
  <done>04 §67–§70's triage API exists, staff-gated, ordered by severity, and speaking the house error envelope.</done>
</task>

<task type="auto">
  <name>Task 3: Django Admin triage — ReportAdmin bulk actions + staff-safe User action</name>
  <files>apps/moderation/admin.py, apps/moderation/tests/test_admin_actions.py</files>
  <read_first>
    - 08_MODERATION.md §9, §9.1 (ReportAdmin spec: columns, filters, search, readonly, actions)
    - apps/accounts/admin.py (existing UserAdmin — extend, don't replace)
    - apps/moderation/services.py (the actions delegate here — no independent update() paths)
  </read_first>
  <action>
    1. `apps/moderation/admin.py` — `ReportAdmin` per §9.1: `list_display` (id, target_type,
       reason, status, reporter, created_at, reviewed_by), `list_filter` (status, reason,
       created_at), `search_fields` (reporter email, description, post title, comment body),
       `readonly_fields` (id, reporter, post, comment, created_at, updated_at), `raw_id_fields`
       or linked FKs for post/comment. Bulk actions, all delegating to Task 1's services inside
       `transaction.atomic()` with the acting admin from `request.user`:
       `dismiss_reports` (§9.1 verbatim behavior via resolve_report), `soft_delete_and_resolve`
       (§9.1's resolve + tombstone via resolve_report REMOVE_CONTENT), `lock_posts`
       (LOCK_POST), `warn_users` (WARN_USER), `ban_users` (BAN_USER, permanent).
       Skip-and-report pattern per §14.1: an action never hard-deletes; already-resolved reports
       are skipped and counted in the action's message.
    2. Extend `apps/accounts/admin.py` UserAdmin: `banned_until` visible in list/detail; a
       `ban_selected_users` action (permanent, requires reason via a short intermediate form or
       fixed audit string "admin bulk ban") and `unban_selected_users`, both calling
       `ban_user`/`unban_user` with `request.user` as moderator; refusing staff members exactly
       like the service does.
    3. No new models, no migration beyond Task 1's.
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c "python -m pytest apps/moderation/tests/test_admin_actions.py -q && python manage.py check"</automated>
    <fails_when>An admin action resolves a report without writing reviewed_by/at; an action hard-deletes content; the User ban action succeeds on a staff member; admin.py import breaks manage.py check.</fails_when>
  </verify>
  <acceptance_criteria>
    - Bulk dismiss on 3 mixed reports → all DISMISSED with reviewed_by/reviewed_at set; already-
      terminal reports skipped with a skip count in the action message.
    - Bulk soft-delete+resolve tombstones each target (is_deleted=True, title/body retained),
      resolves the reports, notifies each author once.
    - Bulk ban action → each target user banned through services.ban_user (log line emitted,
      sever task dispatched); staff members refused.
    - UserAdmin shows banned_until; unban action reactivates without touching devices.
  </acceptance_criteria>
  <done>MOD-05's Django Admin surface exists with bulk triage actions that cannot drift from the REST semantics (same service layer).</done>
</task>

<task type="auto">
  <name>Task 4: Announcement model, expiry + broadcast tasks, beat entries</name>
  <files>apps/community/models.py, apps/community/migrations/0002_announcement.py, apps/community/tasks.py, apps/notifications/tasks.py, config/celery.py, config/settings/base.py, apps/community/tests/test_announcement_broadcast.py</files>
  <read_first>
    - 08_MODERATION.md §7, §7.1 (lifecycle + model verbatim), §7.2 (publish → broadcast trigger)
    - .planning/phases/TCS-JL-06.2-device-registration-fcm-push/06.2-CONTEXT.md (D13–D16: preference gating semantics, zero-PII push)
    - apps/notifications/tasks.py (send_push_notification's ANNOUNCEMENT gating — reuse, don't duplicate), apps/notifications/services.py (create_notification)
    - config/celery.py (the RESERVED `clean-expired-announcements-hourly` entry — name is law)
  </read_first>
  <action>
    1. `apps/community/models.py`: `Announcement` per 08 §7.1 verbatim — UUID pk, `created_by`
       (FK, SET_NULL, null), title(255), body TextField, `is_published` (indexed),
       `is_pinned` (indexed), `published_at`, `expires_at`, timestamps; `ordering =
       ["-is_pinned", "-published_at"]`; the §7.1 composite index (name ≤30 chars, house-style
       `idx_announce_pub_pin_pubat`); `__str__ = title`. Migration `0002_announcement`.
    2. `apps/community/tasks.py` (new module): `clean_expired_announcements` — the beat entry
       exists for the name `community.tasks.clean_expired_announcements`; the task unpublishes
       (is_published=False) published announcements whose `expires_at < now`, logs the count,
       returns it. Byte-exact decorator `name="community.tasks.clean_expired_announcements"`.
    3. `apps/notifications/tasks.py`: `broadcast_announcement(announcement_id)` — decorator
       `name="notifications.tasks.broadcast_announcement"` (byte-matches the existing
       CELERY_TASK_ROUTES entry; notifications queue). Behavior: load the announcement (404-log
       + return if missing); for every active, verified user (`is_active=True, is_verified=True`,
       `.iterator()` for memory): create the in-app ANNOUNCEMENT notification unconditionally
       (D7 — title/body snapshot from the announcement via `create_notification`, which also
       enqueues the preference-gated zero-PII push through the existing ANNOUNCEMENT template
       path). Chunked fan-out: process users in pages of `settings.ANNOUNCEMENT_PUSH_CHUNK`
       (default 500, T6.8's batch bound) logging per-chunk progress. Publishing path (§7.2):
       `Announcement.publish()` model method sets `is_published=True`, `published_at=now` and
       dispatches the task — the single sanctioned trigger (Admin save and the REST publish
       action both route through it; never a post_save signal, per 6.1's import-time-signal
       hazard).
    4. `config/celery.py`: add `"reinstate-suspended-users-hourly"` →
       `moderation.tasks.auto_reinstate_users`, `crontab(minute=25)` (staggered from the
       existing :15/:45 entries), and leave `clean-expired-announcements-hourly` byte-identical.
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c "python -m pytest apps/community/tests/test_announcement_broadcast.py -q && python -c \"import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.local'); django.setup(); from notifications.tasks import broadcast_announcement; assert broadcast_announcement.name == 'notifications.tasks.broadcast_announcement'; from community.tasks import clean_expired_announcements; assert clean_expired_announcements.name == 'community.tasks.clean_expired_announcements'; print('route names OK')\""</automated>
    <fails_when>Task names do not byte-match the reserved beat/route entries; the expiry task deletes rows instead of unpublishing; broadcast creates in-app rows only for preference-opted-in users (that gate belongs to the push task) or pushes announcement body text into lock-screen payloads.</fails_when>
  </verify>
  <acceptance_criteria>
    - `clean_expired_announcements` unpublishes only expired+published rows and returns the
      count; unexpired and draft rows untouched.
    - `broadcast_announcement` creates one ANNOUNCEMENT row per active verified user (a user
      with notify_on_announcements=False still gets the in-app row), and enqueues push through
      send_push_notification, which skips the push for that user (gating proven).
    - Push payload carries the generic ANNOUNCEMENT template text — never announcement body or
      any user identifier (zero-PII, 6.2 D5).
    - `publish()` dispatches exactly one broadcast task per publication; re-publish is a no-op
      (idempotent guard: dispatch only on the False→True transition).
  </acceptance_criteria>
  <done>The announcement substrate (model, publish trigger, broadcast, expiry) exists with byte-exact reserved task names.</done>
</task>

<task type="auto">
  <name>Task 5: Announcement REST CRUD (04 §71–§75)</name>
  <files>apps/community/serializers.py, apps/community/views.py, apps/community/urls.py, apps/community/tests/test_announcements_api.py</files>
  <read_first>
    - 04_API_SPECIFICATION.md §71–§75 (shapes: public list fields; admin-only writes)
    - apps/community/views.py (lock/pin views' IsModerator pattern), apps/community/urls.py (str:pk + JSON-envelope convention)
    - apps/community/models.py (Announcement from Task 4)
  </read_first>
  <action>
    1. `apps/community/serializers.py`: `AnnouncementPublicSerializer` (id, title, body,
       is_pinned, published_at, expires_at — §72's shape exactly, no created_by) and
       `AnnouncementWriteSerializer` (title, body, is_pinned, expires_at; `is_published` set
       via the publish action, not the write serializer — drafts default unpublished).
    2. `apps/community/views.py`:
       - `AnnouncementListCreateView` — GET: AllowAny, only `is_published=True` AND
         (`expires_at IS NULL OR expires_at > now`), §72's envelope + global pagination, pinned
         first (model ordering); POST: IsModerator — creates a draft (201, §73).
       - `AnnouncementDetailView` — PATCH (IsModerator; field edits; a draft→published flip via
         `publish()` when `is_published` transitions, §74), DELETE (IsModerator; §75 — soft is
         unnecessary for announcements; direct delete is allowed and logs the action).
       - `AnnouncementPublishView` — POST `announcements/{id}/publish/`: Idempotent publish
         trigger → `publish()` → 202 {id, is_published: true, detail: "broadcast scheduled"}
         (the broadcast is async; D1-consistent response style).
    3. `apps/community/urls.py`: `announcements/`, `announcements/<str:pk>/`,
       `announcements/<str:pk>/publish/` (the include prefix yields /api/v1/announcements/ per
       §71). Config/urls.py needs no change (community include already at /api/v1/).
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c "python -m pytest apps/community/tests/test_announcements_api.py -q"</automated>
    <fails_when>An anonymous or non-staff user can create/update/delete an announcement; an expired announcement appears in the public list; the public list exposes created_by; GET creates a draft.</fails_when>
  </verify>
  <acceptance_criteria>
    - Anonymous GET → 200 with only published, non-expired rows, pinned first, §72 fields only.
    - Candidate POST/PATCH/DELETE → 403; staff POST → 201 draft; publish → 202 and the row
      appears publicly; expired rows vanish from GET without any delete call.
    - PATCH by staff on a published row updates fields without re-broadcasting; publishing a
      second time does not re-broadcast (idempotent).
  </acceptance_criteria>
  <done>04 §71–§75's announcement API is complete with the draft→publish lifecycle.</done>
</task>

<task type="auto">
  <name>Task 6: Integration suites — triage end-to-end, ban severing, broadcast, gates</name>
  <files>apps/moderation/tests/test_review_api.py, apps/moderation/tests/test_ban_workflow.py, apps/community/tests/test_announcements_api.py, apps/community/tests/test_announcement_broadcast.py</files>
  <read_first>
    - apps/moderation/tests/conftest.py (reporter/author/post/comment fixtures), apps/community/tests/conftest.py (moderator fixture pattern, make_user)
    - apps/notifications/tests/conftest.py (recording-push-backend fixture pattern to reuse)
    - 08_MODERATION.md §12 (moderation test inventory), 04_API_SPECIFICATION.md §107 (required test names)
  </read_first>
  <action>
    1. Cross-app integration tests (in the two moderation test files):
       - The full triage loop over HTTP: candidate reports a scam-adjacent post → staff lists it
         (PENDING, severity-ordered) → REMOVE_CONTENT review → tombstone + author MODERATION
         notification + report RESOLVED → reporter's pending-dedup no longer applies (8.1's
         constraint is PENDING-only, so the same reporter may re-report the tombstoned target).
       - BAN_USER review → author banned → sever task run → author's refresh token replay
         rejected (blacklist), author login → 403 ACCOUNT_SUSPENDED, author's active devices
         gone; unban → login works again, devices still gone.
       - Temporary ban: ban duration_days=7 → auto_reinstate_users run → user reinstated
         (banned_until consumed); permanent ban survives the same run.
       - §107 name coverage: test_admin_can_review_report, test_regular_user_cannot_review_report,
         test_remove_content live in test_review_api.py.
    2. Broadcast end-to-end: publish an announcement via the REST publish endpoint → broadcast
       task (called eagerly) → every active verified user holds an in-app ANNOUNCEMENT row →
       with the recording push backend, pushes fanned out; a user with
       notify_on_announcements=False + push_enabled has the row but no push.
    3. Celery wiring test: assert `app.conf.beat_schedule` contains the three entries with
       byte-exact task names (`community.tasks.clean_expired_announcements`,
       `moderation.tasks.auto_reinstate_users`, plus the untouched analytics/prune entries), and
       that `CELERY_TASK_ROUTES` covers both new moderation tasks.
    4. Full gates: whole pytest suite (694 baseline + new), `ruff check apps config`,
       `ruff format --check apps config`, `makemigrations --check --dry-run` clean AFTER the two
       new migrations exist (0002_user_banned_until, 0002_announcement), `python manage.py check`.
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c "python -m pytest -q && ruff check apps config && ruff format --check apps config && python manage.py makemigrations --check --dry-run && python manage.py check"</automated>
    <fails_when>Any prior-phase test regresses (694 baseline), ruff is unclean, migration drift appears, or a beat entry name mismatches its task.</fails_when>
  </verify>
  <acceptance_criteria>
    - The report→triage→tombstone→notify loop and the ban→sever→403→unban loop both pass end-to-end
      over HTTP with real transaction + task boundaries.
    - Every §107 moderation test name exists and passes; every CONTEXT D1–D9 decision has a
      named passing test asserting it.
    - Full suite green (694 + new); lint/format clean; exactly two new migrations, no drift.
  </acceptance_criteria>
  <done>MOD-05 and MOD-06 are proven end-to-end; the phase's done-when (banned accounts lose sessions and device alerts, with the severing atomic-at-the-transaction per D1) holds.</done>
</task>

<task type="auto">
  <name>Task 7: Tracking updates — requirements, roadmap, state</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (MOD section + traceability format), .planning/ROADMAP.md (Phase 8.2 section), .planning/STATE.md (Decisions/Blockers format)
  </read_first>
  <action>
    1. REQUIREMENTS.md: mark MOD-05 Complete (triage over REST + Admin with the five actions,
       severity ordering, velocity multiplier diverged per D6) and MOD-06 Complete (ban protocol:
       transaction + idempotent Celery severing per D1, temporary + permanent per D3,
       ACCOUNT_SUSPENDED login); update the 8.2 traceability row.
    2. ROADMAP.md: tick plan 08-03 with the execution date; record Phase 8.2 executed with the
       scope summary (five actions, ban protocol, announcements full scope, two new migrations);
       note the done-when language: sessions/device severing completes via the idempotent Celery
       step (08 §6's own shape), atomic at the user-facing transaction (D1).
    3. STATE.md: record the phase decisions (D1–D9 headlines), the new Pending-Todo pointers
       (none new unless execution surfaces one), update Current Position to 8.2 executed /
       UAT next, and add to Deferred: velocity multiplier, AuditLog model, day-90 minimization,
       unban-does-not-reactivate-devices (D2).
  </action>
  <verify>
    <automated>grep -c "MOD-05" .planning/REQUIREMENTS.md && grep -c "08-03" .planning/ROADMAP.md</automated>
    <fails_when>Tracking files contradict the shipped state (MOD-05/06 not Complete, 08-03 unticked).</fails_when>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md shows MOD-05 and MOD-06 Complete with accurate summaries.
    - ROADMAP.md ticks 08-03 and states Phase 8.2 executed with the D1 done-when language.
    - STATE.md Current Position points at 8.2 UAT/verify as the next step.
  </acceptance_criteria>
  <done>Tracking docs match shipped reality.</done>
</task>

</tasks>

<threat_model>
**T-08.2-01 (HIGH): Banned user keeps live sessions across the async window (D1's accepted gap).**
The transaction commits before severing; a crash strands sessions/devices. Mitigation: the sever
task is idempotent and re-runnable; re-banning re-fires it; §6.2's 401-on-inactive access tokens
already close the API path the moment the flag lands. Verified by: sever-task idempotence tests +
token-replay-rejected tests.
**T-08.2-02 (HIGH): Privilege escalation through the moderation surface.** A non-staff caller
reaching ban/review, or a ban targeting a staff member. Mitigation: IsModerator on every endpoint,
`is_staff` refusal inside `ban_user` itself (defense in depth), ban responses expose no
is_staff/is_superuser writable field (04 §70). Verified by: 403 tests + staff-target-refusal test.
**T-08.2-03 (MEDIUM): Moderation metadata leak.** `moderator_notes`, `reviewed_by`, reporter
identity reaching ordinary users. Mitigation: ModerationReportSerializer carries §68's fields
only; review/ban responses return action/status/id. Verified by: response-shape assertions.
**T-08.2-04 (MEDIUM): Admin bulk actions drifting from REST semantics.** Copy-pasted update()
logic in admin would bypass logging/validation. Mitigation: every admin action delegates to
`moderation.services`. Verified by: admin-action tests asserting log lines and reviewed_by writes.
**T-08.2-05 (LOW): Announcement push PII leak.** Body text or user identifiers in lock-screen
payloads. Mitigation: broadcast reuses create_notification + the fixed ANNOUNCEMENT push template.
Verified by: payload assertions on the recording backend.
**T-08.2-06 (LOW): Reserved-name drift.** A task name off by one byte silently drops its queue
route or beat entry (the 7.2/6.2 lesson, twice recorded). Mitigation: byte-exact names + a wiring
test asserting beat_schedule and CELERY_TASK_ROUTES. Verified by: the Task 6 wiring test.
</threat_model>

<verification>
- Full test suite green (694 baseline + new suites); ruff check + format clean; exactly two new
  migrations (0002_user_banned_until, 0002_announcement) and zero drift; `manage.py check` clean.
- GET /api/v1/moderation/reports/ is staff-only, §68-shaped, severity-ordered; review executes all
  five actions with correct status transitions and reviewed_by/at/notes writes.
- Ban protocol: transaction sets is_active (+banned_until for temporary) with the §11.1 log line;
  the sever task idempotently blacklists tokens and deactivates devices; login returns 403
  ACCOUNT_SUSPENDED; unban reactivates without reviving devices; auto_reinstate_users flips only
  expired temporary bans.
- Django Admin: ReportAdmin bulk actions (dismiss / soft-delete+resolve / lock / warn / ban) all
  delegate to the services layer; UserAdmin gains a staff-safe ban/unban action.
- Announcements: public list shows published non-expired rows pinned-first; staff CRUD + publish
  → broadcast (byte-exact `notifications.tasks.broadcast_announcement`) creates unconditional
  in-app rows and preference-gated zero-PII pushes; `clean_expired_announcements` unpublishes
  expired rows on its reserved hourly beat.
</verification>

<success_criteria>
- MOD-05 delivered: staff triage over REST and Django Admin with DISMISS / REMOVE_CONTENT /
  LOCK_POST / WARN_USER / BAN_USER bulk-capable actions per CONTEXT D4–D6.
- MOD-06 delivered: banning sets is_active=False, blacklists refresh tokens, and halts device push
  alerts — the transaction+task shape of 08 §6 (D1), temporary and permanent (D3), with the
  suspended-login contract of §6.2.
- The 6.2 D15 deferral is closed: announcements ship end-to-end (model, REST, broadcast, expiry)
  with reserved task names byte-exact (D7).
- Phase 8.2 done-when holds: banned accounts lose sessions and device alerts — atomically at the
  user-facing transaction, via the spec's own async severing step.
</success_criteria>
