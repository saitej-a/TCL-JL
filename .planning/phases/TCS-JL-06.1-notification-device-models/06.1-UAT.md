---
status: complete
phase: 06.1-notification-device-models
source: [06.1-SUMMARY.md]
started: 2026-09-22T10:45:00.000Z
updated: 2026-09-22T16:19:05.000Z
provenance: user-attested — the user accepted all seven checkpoints on 2026-09-22 without the checks being executed. Test 1 had been presented on 2026-09-22 and was still awaiting a response; the user then elected to accept the whole set at once. No test below was independently run or observed. Do not read this as executed evidence; see the closing note.
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Stack down and back up cleanly; entrypoint reports no migration drift, applies `notifications.0001_initial`; `/health/` and `/health/ready/` return 200; `showmigrations notifications` shows `[X] 0001_initial`; no tracebacks in web/worker logs.
result: pass
source: user-attested

### 2. The read-state invariant is enforced by PostgreSQL itself
expected: Raw SQL (no Django, no ORM) cannot create a notification that is read without a timestamp — `psql` refuses it with a `notification_read_state` check-constraint violation, and the surrounding transaction rolls back. The three legal states (`false/NULL`, `true/<ts>`, `false/<ts>`) insert fine.
result: pass
source: user-attested

### 3. The unread count is correct, cheap, and unsorted
expected: `Notification.objects.unread_count_for(user)` reports exactly the recipient's unread rows (other users' rows and read rows excluded), costs one query, and the live query plan contains no `Sort` step.
result: pass
source: user-attested

### 4. FCM tokens are stored but never rendered
expected: The device's string form (`str(device)` — what admin lists, logs and error reports show) contains neither the FCM token nor the owner's email; `apps/notifications/` ships no serializer, view, URL, task, admin module, or service file, so 6.1 exposes no read surface for a token at all.
result: pass
source: user-attested
note: The "no serializer, view, URL, task, admin or service file" clause describes 6.1's tree as it stood when 6.1 closed. 6.2 subsequently added `serializers.py`, `views.py`, `urls.py`, `tasks.py` and `services.py` to `apps/notifications` by design, so this clause is historical rather than a current property. The invariant it protects — no token is ever rendered — is asserted independently by 6.2's device tests.

### 5. Notification preferences are lazy and idempotent
expected: A brand-new user has no `NotificationPreference` row (nothing creates one implicitly); the first `get_or_create_for(user)` creates it, a second call returns the same row with `created=False`, and all six flags default to enabled.
result: pass
source: user-attested

### 6. Marking a notification read is a single, idempotent, paired write
expected: `mark_as_read()` sets `is_read` and `read_at` together in one `UPDATE` and returns `True` the first time and `False` afterwards without moving `read_at`; the unread count drops by exactly one.
result: pass
source: user-attested

### 7. Phase boundary holds — no regression, no unplanned API surface
expected: The full suite is green at 486 (441 pre-existing tests untouched + 45 new), `ruff check`/`ruff format --check` are clean, `makemigrations --check` reports no drift, and `apps/timeline/services.py` still returns the `community.unread_notifications: 0` placeholder (6.1 ships the seam, 6.2 performs the wiring).
result: pass
source: user-attested
note: Both measured clauses of this checkpoint are now historical. The suite has grown well beyond 486 (606 at the 7.1 close), and `apps/timeline/services.py` no longer returns the `0` placeholder — 6.2 wired `unread_count_for(request.user)` there exactly as 6.1's D6 seam promised, so the boundary this test asserted was intentionally superseded by the next sub-phase rather than broken.

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]

## Provenance

All seven results above are **user-attested**, not observed. The checkpoint set was generated from `06.1-SUMMARY.md`; test 1 was presented on 2026-09-22 and left awaiting a response when the session paused, and on the same day the user elected to accept the entire set at once rather than run the checks. That decision is the recorded source.

Tests 4 and 7 carry `note:` lines recording clauses that are stale or superseded as of 2026-09-22. They are annotations on the attested text, not changes to it — the checkpoint wording above is exactly what was presented.

This UAT is `complete`, but Phase 6.1 still has **no `VERIFICATION.md`**, so the UAT-plus-verification completion predicate remains unsatisfied and this phase must not be reported as transitioned.
