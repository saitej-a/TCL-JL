---
phase: 6.1
slug: notification-device-models
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-09-22
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + pytest-django (Django 5.2.6) |
| **Config file** | `pytest.ini` (`config.settings.test`, `--reuse-db -v`) |
| **Quick run command** | `docker compose run --rm web sh -c "pip install -q -r requirements-dev.txt && python -m pytest apps/notifications -q"` |
| **Full suite command** | `docker compose run --rm web sh -c "pip install -q -r requirements-dev.txt && python -m pytest -q"` |
| **Estimated runtime** | ~10–20 s subset · ~60–120 s full (441+ pre-existing) |

---

## Sampling Rate

- **After every task commit:** Run the quick run command (notifications subset)
- **After every plan wave:** Run the full suite command + `ruff check apps config` + `ruff format --check apps config` + `makemigrations --check --dry-run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-01-01 | 06-01 | 1 | NOTIF-01 | T-6.1-01 | Model layer only; no token exposure surface | unit | pytest apps/notifications | ❌ W0-equivalent (scaffold task) | ⬜ pending |
| 06.1-01-02 | 06-01 | 1 | NOTIF-01 | T-6.1-02 | `notification_read_state` constraint blocks inconsistent read state at DB level | unit + DB drill | pytest + psql drill | ❌ (created by task) | ⬜ pending |
| 06.1-01-03 | 06-01 | 1 | NOTIF-01 | — | Index support for unread-count + list queries | unit (migration assertions) + psql | pytest + psql drill | ❌ (created by task) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] pytest + pytest-django installed (`requirements-dev.txt`)
- [x] `pytest.ini` configured (`config.settings.test`)
- Existing infrastructure covers all phase requirements; the app scaffold + its test package are Task 1 of plan 06-01 (Wave 0-equivalent).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| (none) | — | Model layer is fully automatable; the live psql/shell drill is scripted in the plan's Stage 2 | — |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — infra exists)
- [x] No watch-mode flags
- [x] Feedback latency < 120 s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-09-22
