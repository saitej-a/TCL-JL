---
status: complete
phase: 06.2-device-registration-fcm-push
source: [06.2-SUMMARY.md]
started: 2026-09-22T14:00:00.000Z
updated: 2026-09-22T16:19:05.000Z
provenance: user-attested — the user accepted all ten checkpoints on 2026-09-22 without the checks being executed. No test below was independently run or observed. Do not read this as executed evidence; see the closing note.
---

## Current Test

[testing complete]

## Tests

### 1. Device Registration and Token Redaction
expected: |
  POST /api/v1/devices/ with fcm_token, device_type="WEB", and browser returns HTTP 201.
  The response JSON contains id, device_type, browser, is_active, and last_seen_at, but strictly omits fcm_token.
  GET /api/v1/devices/ lists active devices and also never exposes fcm_token.
result: pass
source: user-attested

### 2. Device Revocation Soft-Deactivates
expected: |
  DELETE /api/v1/devices/{id}/ returns HTTP 204 No Content.
  The device row remains in the database with is_active=False.
  Subsequent GET /api/v1/devices/ excludes this revoked device.
result: pass
source: user-attested

### 3. Comment Event Creates In-App Notification and Enqueues Push
expected: |
  When candidate A comments on candidate B's post, an in-app Notification row is created for B with title and message.
  B's GET /api/v1/notifications/ returns the notification with unread_count=1.
  A background Celery push task is enqueued with generic template text carrying zero PII (no comment body or user name on lock screen).
result: pass
source: user-attested

### 4. Self-Action Suppression
expected: |
  When a candidate comments on their own post or replies to their own comment, no notification row is created and no push task is enqueued.
result: pass
source: user-attested

### 5. Mark Single and Mark All Notifications As Read
expected: |
  POST /api/v1/notifications/{id}/read/ marks the notification read (is_read=True, read_at set) and drops unread_count.
  POST /api/v1/notifications/read-all/ marks all unread notifications for the user as read and returns updated_count.
result: pass
source: user-attested

### 6. Notification Preferences Lazy Creation and PATCH
expected: |
  A fresh user has no preferences row until GET /api/v1/notifications/preferences/ is requested.
  GET creates it lazily with all push flags defaulted to true.
  PATCH /api/v1/notifications/preferences/ allows updating push flags (e.g. push_enabled=False).
result: pass
source: user-attested

### 7. Upvote Milestone Notification at 10 Upvotes
expected: |
  When a post crosses the 10th upvote threshold via the voting endpoint, a VOTE_MILESTONE in-app notification is generated for the post author.
  Duplicate crossing attempts do not spawn duplicate milestone alerts.
result: pass
source: user-attested

### 8. Dashboard Exposes Real Unread Notification Count
expected: |
  GET /api/v1/dashboard/ returns community.unread_notifications reflecting the candidate's actual unread notification count rather than the 0 placeholder.
result: pass
source: user-attested

### 9. Thread Push 15-Minute Debounce
expected: |
  Multiple rapid comments on the same post within a 15-minute window create in-app notification rows for every comment, but suppress subsequent push alerts via Redis/cache debouncing.
result: pass
source: user-attested

### 10. Automated Suite & Phase Boundary Integrity
expected: |
  The full pytest test suite passes completely (584 passed), makemigrations --check reports no drift, and ruff checks pass cleanly.
result: pass
source: user-attested

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]

## Provenance

All ten results above are **user-attested**, not observed. The checkpoint set was generated from `06.2-SUMMARY.md` and never presented one at a time; on `2026-09-22` the user elected to accept all of them in a single response rather than run the checks, and that decision is the recorded source.

Test 10's expected figure (`584 passed`) was written when 6.2 closed the suite at that size; the suite has since grown (7.1 alone added 22 tests). The count has not been re-measured for this attestation.

This UAT is `complete`, but Phase 6.2 still has **no `VERIFICATION.md`**, so the UAT-plus-verification completion predicate remains unsatisfied and this phase must not be reported as transitioned.
