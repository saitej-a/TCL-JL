---
phase: 09.2-auth-onboarding-and-layout-views
plan: 09.2-01
type: execute
wave: 1
depends_on: [09.1-01]
files_modified:
  - apps/accounts/serializers.py
  - apps/accounts/tests/test_me.py
  - frontend/src/api/profile.ts
  - frontend/src/api/timeline.ts
  - frontend/src/api/announcements.ts
  - frontend/src/api/stats.ts
  - frontend/src/types/user.ts
  - frontend/src/types/timeline.ts
  - frontend/src/hooks/useProfile.ts
  - frontend/src/layouts/AppShell.tsx
  - frontend/src/layouts/MobileTabBar.tsx
  - frontend/src/components/AnnouncementBanner.tsx
  - frontend/src/components/SidebarNav.tsx
  - frontend/src/pages/LandingPage.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/RegisterPage.tsx
  - frontend/src/pages/ForgotPasswordPage.tsx
  - frontend/src/pages/VerifyEmailPendingPage.tsx
  - frontend/src/pages/VerifyEmailActionPage.tsx
  - frontend/src/pages/ResetPasswordPage.tsx
  - frontend/src/pages/OnboardingPage.tsx
  - frontend/src/pages/*.tsx (stubs become real or get chrome)
  - frontend/src/routes/*.tsx (guards + router updates)
  - frontend/src/components/*.test.tsx
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [UI-01, UI-04]
user_setup:
  - action: "Compose stack must be running (docker compose up -d) for the backend task and live proofs."
    when: "before Task 1 and Task 6"
must_haves:
  truths:
    - "D1 (CONTEXT): profile_completed is computed from real CandidateProfile state (profile row exists with hiring_type, role, region_of_joining, batch and offer_date present) — no hardcoded False; the existing `is False` assertion in test_me.py is updated deliberately to assert the real behavior in both states."
    - "D2 (CONTEXT): no staff navigation ships — no staff links, no is_staff display, no staff-only UI gating; the O1 /admin collision decision is untouched."
    - "D3 (CONTEXT): the §5.5 announcement banner exists, is fed by GET /api/v1/community/announcements/, dismiss is persisted, and it renders above the shell at every breakpoint."
    - "D4 (CONTEXT): every 9.2 screen has an approved Stitch mockup behind it (10 screens, design system assets/9887579562818178405); Stitch HTML is reference, never copied wholesale — implementation uses the 9.1 token layer and the spec's class strings."
    - "UI-01: the authenticated shell is 3-column desktop (§5.2), 2-column tablet (§5.3), mobile top-bar + bottom tab bar (§5.4, 5 tabs, ≥44px targets), with the disclaimer present (the §5.6 footer string) at every breakpoint — the roadmap done-when."
    - "UI-04: six auth screens (§7.2.1–7.2.6) and the 3-step wizard (§7.3) work against the real endpoints; the wizard writes profile fields through POST /profile/ and step-2 status through POST /timeline/ (walk-the-chain auto-sync)."
    - "05 §3.1 gating: profile_completed=false routes a signed-in user from protected surfaces into /onboarding; completing the wizard refreshes the user and releases the gate."
  artifacts:
    - path: "apps/accounts/serializers.py"
      provides: "the truthful profile_completed computation + updated tests"
      contains: "profile_completed"
    - path: "frontend/src/layouts/AppShell.tsx"
      provides: "the responsive shell: sidebar (desktop), rail, top bar, mobile tab bar, banner slot, footer disclaimer"
      contains: "SidebarNav"
    - path: "frontend/src/components/AnnouncementBanner.tsx"
      provides: "the D3 banner with dismiss persistence"
      contains: "announcements"
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "§7.1 hero + live stats from GET /api/v1/public/stats/"
      contains: "public/stats"
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "§7.2.1 login with error mapping (INVALID_CREDENTIALS, RATE_LIMITED + Retry-After, ACCOUNT_SUSPENDED)"
      contains: "INVALID_CREDENTIALS"
    - path: "frontend/src/pages/OnboardingPage.tsx"
      provides: "the §7.3 3-step wizard with per-step persistence and review"
      contains: "current_status"
  key_links:
    - from: "apps/accounts/serializers.py"
      to: "apps/candidates/models.py CandidateProfile"
      via: "profile_completed reads the real profile row"
      pattern: "CandidateProfile"
    - from: "frontend/src/pages/OnboardingPage.tsx"
      to: "frontend/src/api/profile.ts + frontend/src/api/timeline.ts"
      via: "step 1 persists via POST /profile/, step 2 via POST /timeline/"
      pattern: "profile|timeline"
    - from: "frontend/src/layouts/AppShell.tsx"
      to: "frontend/src/components/Disclaimer"
      via: "the footer renders FOOTER_DISCLAIMER — never retyped"
      pattern: "FOOTER_DISCLAIMER"
---

<objective>
Deliver UI-01's layout shells and UI-04's auth + onboarding journey (roadmap plan 09-02), binding the
9.1 foundation to the real screens: the responsive AppShell (3-col desktop / 2-col tablet / mobile
tab-bar) with the §5.5 announcement banner and the §5.6 footer disclaimer, the §7.1 landing page with
live stats, the six §7.2 auth screens, and the §7.3 3-step onboarding wizard — plus one explicit
backend task: make `profile_completed` truthful (D1), because the wizard's gate flag is currently a
hardcoded `False`.

User decisions binding this plan (09.2-CONTEXT.md): D1 (fix the profile_completed stub as a real
backend task with tests), D2 (defer staff navigation entirely — no staff links, no is_staff display;
O1 moves to the staff-UI phase), D3 (include the §5.5 banner fed by the live announcements endpoint,
with dismiss persistence), D4 (Stitch is the design source — 10 mockups generated from design system
assets/9887579562818178405; screen IDs in 09.2-CONTEXT.md; output is visual reference, not code).

Design assets: Stitch project 3852118218307261541 — App Shell (light 76e2fbb0…, dark d717d1f5…),
Mobile Shell d0b7036c…, Landing e264860e…, Login cffe3468…, Registration f6078b3f…,
Forgot Password 94c94c80…, Onboarding Steps 1–3 (8fd5a51a…, fd3f9c37…, 952bd84a…). The three
unmocked auth variants (verify-email, resend, reset) follow the established auth-card pattern.

Plan-time resolutions of the three open questions (from 09.2-CONTEXT.md):
- Gating mechanism: **redirect** — RequireAuth checks `user.profile_completed`; when false it sends
  protected surfaces to /onboarding (matches §7.3's "follows login" framing).
- Banner dismiss: **per-announcement ID** in localStorage (`tjt.dismissed_announcements`) — a new
  announcement is not hidden by an old dismissal.
- Wizard write order: **per-step persistence** (step 1 POST /profile/, step 2 POST /timeline/) so a
  mid-wizard reload never loses entered data; step 3 is review + confirm.

Explicitly out of scope: dashboard/timeline/feed content (9.3), post detail/analytics/notifications/
PWA (9.4), staff UI (deferred, D2), OpenAPI type generation, CI wiring.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/TCS-JL-09.2-auth-onboarding-and-layout-views/09.2-CONTEXT.md
@05_UI_UX_SPECIFICATION.md
@04_API_SPECIFICATION.md
@11_AI_AGENT_RULES.md
@apps/accounts/serializers.py
@apps/candidates/serializers.py
@apps/candidates/urls.py
@apps/community/urls.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — make profile_completed truthful (D1)</name>
  <files>apps/accounts/serializers.py, apps/accounts/tests/test_me.py</files>
  <read_first>
    - apps/accounts/serializers.py (UserPrivateSerializer.get_profile_completed — the hardcoded False)
    - apps/candidates/models.py (CandidateProfile fields: hiring_type, role, region_of_joining, batch,
      offer_date — confirm exact field names before writing the check)
    - apps/accounts/tests/test_me.py (the `is False` assertion to replace)
  </read_first>
  <action>
    1. Compute profile_completed from real state: the requesting user has a CandidateProfile row AND
       the fields the wizard collects are present (hiring_type, role, region_of_joining, batch,
       offer_date — confirm names against the model first). No other behavior change: the field stays
       a read-only serializer computation, no migration, no new endpoint.
    2. Update apps/accounts/tests/test_me.py deliberately: with no profile → False; with a complete
       profile → True; with a partial profile (a missing wizard field) → False. Keep/adjust any
       existing assertions that encoded the stub.
    3. Run the accounts test module; then the full suite in Task 6.
  </action>
  <verify>
    <automated>docker compose exec -T web sh -c "python -m pytest apps/accounts/tests/test_me.py -q"</automated>
    <fails_when>profile_completed still returns a constant; an existing test asserting False is left failing or was deleted instead of updated; the full suite regresses in Task 6.</fails_when>
  </verify>
  <acceptance_criteria>
    - /me/ reports profile_completed=true for a complete profile and false for a missing/partial one,
      proven by tests.
    - The backend change is serializer-only: no migration, no new endpoint, no contract drift.
  </acceptance_criteria>
  <done>The onboarding gate flag reflects reality, with tests pinning both directions.</done>
</task>

<task type="auto">
  <name>Task 2: The responsive AppShell + announcement banner (UI-01)</name>
  <files>frontend/src/layouts/AppShell.tsx, frontend/src/layouts/MobileTabBar.tsx, frontend/src/components/AnnouncementBanner.tsx, frontend/src/components/SidebarNav.tsx, frontend/src/pages/LandingPage.tsx (stub swap), frontend/src/layouts/AppShell.test.tsx, frontend/src/components/AnnouncementBanner.test.tsx</files>
  <read_first>
    - 05_UI_UX_SPECIFICATION.md §5.2 (desktop 3-col), §5.3 (tablet 2-col), §5.4 (mobile top bar +
      bottom tabs, ≥44px), §5.5 (banner), §7.4 (navigation anatomy)
    - Stitch mockups: App Shell light 76e2fbb02799457d9d220daf435d9c2c, dark d717d1f5b6354899a1d806c444e36f82,
      Mobile d0b7036cca764f689569424b5ffdc4a1 (visual reference for proportions and chrome)
    - frontend/src/theme/tokens.ts + badges.ts (consume, never re-type)
  </read_first>
  <action>
    1. `AppShell.tsx` — the authenticated chrome, responsive by container CSS (Tailwind breakpoints):
       - Desktop ≥1024px (§5.2): left sidebar 240px (brand, nav pills, secondary links), center
         column with top bar (search affordance, bell, IdentityPill), right rail 320px with placeholder
         cards (9.3 fills them), sticky footer with `Disclaimer variant="footer"`.
       - Tablet 640–1023px (§5.3): rail collapses; sidebar persists.
       - Mobile <640px (§5.4): top app bar (brand + bell + avatar); bottom tab bar with 5 tabs
         (Dashboard, Timeline, Community, Alerts, Settings), ≥44px touch targets, active tab indigo;
         content padding-bottom clears the fixed bar.
       - The banner renders above the shell at every breakpoint; the footer disclaimer is present at
         every breakpoint (the roadmap done-when).
    2. `SidebarNav.tsx` — the nav list consumed by desktop/tablet; NavLink active state indigo pill;
       no staff links (D2). Items: Dashboard, Timeline, Community, Analytics, Alerts, Settings.
    3. `MobileTabBar.tsx` — the 5-tab bar consumed by mobile; same items minus Analytics (mobile nav
       per §5.4 shows five).
    4. `AnnouncementBanner.tsx` (D3) — fetches `GET /api/v1/community/announcements/` (paginated,
       latest first) via a small `api/announcements.ts`; renders the latest un-dismissed item's notice;
       dismiss (×) persists the announcement ID under localStorage key `tjt.dismissed_announcements`
       (a JSON array); a dismissed ID hides the banner on reload; a NEW announcement id shows it.
       Dismiss state must be resilient to storage failure (try/catch). Renders nothing when the API
       has no announcements or the fetch fails (silent degradation, banner is chrome not content).
    5. Wire the router: authenticated routes render inside AppShell (layout route pattern already used
       by RequireAuth — RequireAuth wraps AppShell); replace the stub pages' placeholder chrome with
       page content slots; `/onboarding` and the auth screens render OUTSIDE the shell (pre-auth
       journey); the landing page renders outside the shell.
    6. Tests: AppShell renders sidebar at desktop and the tab bar at mobile (jsdom viewport stubs or
       class assertions); banner shows the live item and hides after dismiss + reload (localStorage
       assertion); tab items meet the 44px rule (class assertion min-h/min-w); footer disclaimer text
       equals FOOTER_DISCLAIMER.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:run -- src/layouts src/components/AnnouncementBanner.test.tsx &amp;&amp; npm run typecheck</automated>
    <fails_when>A breakpoint renders the wrong chrome; the banner reappears after dismissal on reload; a new announcement ID is hidden by an old dismissal; the disclaimer is retyped instead of imported; staff links exist (D2 violation).</fails_when>
  </verify>
  <acceptance_criteria>
    - One AppShell serves all three breakpoints with the spec'd anatomy; the disclaimer string renders
      from the single content module at every breakpoint.
    - Banner dismissal is per-announcement-ID, persists across reloads, and degrades silently when
      storage or the API fail.
    - No staff navigation anywhere (D2).
  </acceptance_criteria>
  <done>The shell is real, responsive, banner-fed, and disclaimer-pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Landing page with live stats (§7.1)</name>
  <files>frontend/src/pages/LandingPage.tsx, frontend/src/api/stats.ts, frontend/src/pages/LandingPage.test.tsx</files>
  <read_first>
    - 05_UI_UX_SPECIFICATION.md §7.1 (hero, stats band, feature cards, footer)
    - 04_API_SPECIFICATION.md §public/stats (response shape — confirm field names in
      apps/community/views or analytics views before typing)
    - Stitch Landing mockup e264860e6a5d409fabdb1ff53066481f (visual reference)
  </read_first>
  <action>
    1. `api/stats.ts` — typed `getPublicStats()` against the endpoint proven live in 9.1
       (`/api/v1/public/stats/`); type the response from the serializer actually shipped (read it in
       read_first; do not guess field names).
    2. `LandingPage.tsx` — §7.1: nav bar (brand, links, Sign in / Create account CTAs), hero, live
       stats band driven by getPublicStats() (loading skeleton → numbers; on failure render the band
       with EmptyState semantics rather than zeros — never fabricate numbers), feature cards, and the
       footer disclaimer. Existing landing stub content replaced.
    3. Test: stats render after resolve; failure path renders the fallback (no fabricated numbers);
       CTA links route to /register and /login.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:run -- src/pages/LandingPage.test.tsx &amp;&amp; npm run typecheck</automated>
    <fails_when>Stats render fabricated defaults on API failure; the page is unreachable at /; the disclaimer is absent.</fails_when>
  </verify>
  <acceptance_criteria>
    - / renders the §7.1 landing with live stats wired to the real endpoint (graceful failure mode
      included), and the §5.6 footer disclaimer.
  </acceptance_criteria>
  <done>The public entry screen is real and honest about failure.</done>
</task>

<task type="auto">
  <name>Task 4: The six auth screens (§7.2.1–7.2.6)</name>
  <files>frontend/src/pages/LoginPage.tsx, frontend/src/pages/RegisterPage.tsx, frontend/src/pages/ForgotPasswordPage.tsx, frontend/src/pages/VerifyEmailPendingPage.tsx, frontend/src/pages/VerifyEmailActionPage.tsx, frontend/src/pages/ResetPasswordPage.tsx, frontend/src/api/auth.ts (extend if needed), frontend/src/pages/LoginPage.test.tsx, frontend/src/pages/RegisterPage.test.tsx</files>
  <read_first>
    - 05_UI_UX_SPECIFICATION.md §7.2.1–7.2.6 (each screen's fields, states, copy)
    - 04_API_SPECIFICATION.md auth endpoints + apps/accounts/exceptions.py (INVALID_CREDENTIALS,
      RATE_LIMITED with Retry-After, ACCOUNT_SUSPENDED) and the register/verify/reset request shapes
    - Stitch: Login cffe3468…, Registration f6078b3f…, Forgot 94c94c80… (the auth-card pattern)
  </read_first>
  <action>
    1. Shared `AuthCard` layout affordance (component-local to pages, or a small shared component):
       centered card, brand row, per-screen H1/subheader, form rows, error/success strips (rose-50 /
       emerald-50 patterns from the mockups), footer disclaimer — one visual language for all six.
    2. LoginPage (§7.2.1): email+password, show/hide password, submit → auth.login → navigate to
       `next` param when present else /dashboard. Error mapping per code: INVALID_CREDENTIALS (inline
       rose strip "Invalid email or password."), RATE_LIMITED (disable submit + countdown from
       Retry-After), ACCOUNT_SUSPENDED (distinct copy). Unverified accounts (user.is_verified=false)
       land on /verify-email-pending (the §7.2.3 screen) instead of the dashboard.
    3. RegisterPage (§7.2.2): display_name + email + password + confirm + terms checkbox; client-side
       validation before submit; on success navigate to /verify-email-pending with the sent-email
       state.
    4. ForgotPasswordPage (§7.2.5): email → requestPasswordReset → success strip
       ("If that email exists…" — enumeration-safe copy per spec), back-to-sign-in link.
    5. VerifyEmailPendingPage (§7.2.3): "check your inbox" state + resend-with-cooldown button wired
       to resendVerification.
    6. VerifyEmailActionPage (§7.2.4 for the :token visit): calls verifyEmail(token), renders
       success/failure state with the appropriate CTA (to login or resend).
    7. ResetPasswordPage (§7.2.6): new password + confirm against confirmPasswordReset(token), success
       routes to /login.
    8. Tests: login error mapping for all three codes (+ next-param navigation); register validation
       blocks submit with mismatched passwords; forgot-password success strip; verify-email action
       token success/failure states.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:run -- src/pages/LoginPage.test.tsx src/pages/RegisterPage.test.tsx &amp;&amp; npm run typecheck</automated>
    <fails_when>A wrong-password login triggers a refresh call (client contract violation); RATE_LIMITED shows no countdown; an unverified user reaches the dashboard; register submits mismatched passwords; any screen lacks the disclaimer or errors before reaching a success state.</fails_when>
  </verify>
  <acceptance_criteria>
    - All six §7.2 screens exist at their §3.1 routes with the auth-card language, correct endpoint
      wiring, and the three login error codes mapped distinctly.
    - Unverified users are routed to the pending-verification screen, never the dashboard.
  </acceptance_criteria>
  <done>The pre-auth journey is complete and honest about every failure mode.</done>
</task>

<task type="auto">
  <name>Task 5: The 3-step onboarding wizard (§7.3) + the gating wire</name>
  <files>frontend/src/pages/OnboardingPage.tsx, frontend/src/api/profile.ts, frontend/src/api/timeline.ts, frontend/src/types/timeline.ts, frontend/src/hooks/useProfile.ts, frontend/src/routes/RequireAuth.tsx (gating), frontend/src/routes/router.tsx (/onboarding), frontend/src/pages/OnboardingPage.test.tsx</files>
  <read_first>
    - 05_UI_UX_SPECIFICATION.md §7.3 (the three steps, fields, review state)
    - apps/candidates/serializers.py (ProfileSerializer — confirm the exact accepted fields and the
      current_status side-channel) + apps/candidates/urls.py (POST /profile/, POST /timeline/)
    - 04 §walk-the-chain context from Phase 4.1 (which step-2 statuses map to which timeline events)
    - Stitch: Onboarding Steps 1–3 (8fd5a51a…, fd3f9c37…, 952bd84a…)
  </read_first>
  <action>
    1. `api/profile.ts` + `hooks/useProfile.ts` — typed getProfile/updateProfile over the real
       ProfileSerializer fields (byte-exact names); `api/timeline.ts` — createTimelineEvent with the
       04 §event contract and the timeline event vocabulary union.
    2. `OnboardingPage.tsx` — the 3 steps per §7.3 and the mockups: step 1 offer details (hiring_type
       radio-cards Prime/Digital/BPS, role, region select, batch, offer date); step 2 current status
       (radio-card list of the status vocabulary); step 3 review rows with per-row edit + confirm.
       Per-step persistence: step 1 saves via POST (or PATCH if a profile exists — confirm the
       serializer's semantics in read_first) before advancing; step 2 saves the status via the
       timeline event (or the current_status side-channel where no event type exists — follow the
       backend's actual contract); step 3 review + confirm → refreshUser() (the /me/ flag flips) →
       navigate to /dashboard. Mid-wizard reload resumes at the first incomplete step (data persisted
       per step). Progress indicator (3 segments), Skip-for-now affordance per spec, disclaimer below
       the card.
    3. Gating wire: RequireAuth (or the AppShell layout route) checks `user.profile_completed`; when
       false and the route is not /onboarding, redirect to /onboarding; /onboarding itself is behind
       RequireAuth (auth required, no profile required). Completing the wizard calls refreshUser() so
       the gate releases without a full reload.
    4. Tests: gating redirect fires only when profile_completed=false; wizard step 1 posts the real
       field names; step 2 creates the timeline event; completing the wizard calls refreshUser and
       releases the gate; mid-wizard resume renders persisted values.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:run -- src/pages/OnboardingPage.test.tsx src/routes &amp;&amp; npm run typecheck</automated>
    <fails_when>The gate never fires (or fires for completed users); the wizard writes invented field names; step 2 bypasses walk-the-chain semantics; refreshUser is not called on completion; a reload mid-wizard loses data.</fails_when>
  </verify>
  <acceptance_criteria>
    - A signed-in user with profile_completed=false is redirected to /onboarding from protected
      surfaces; completing the three steps persists per-step with real field names, flips the flag
      (refreshUser), and releases the gate without a reload.
    - The wizard follows §7.3's step anatomy and the Stitch mockups' visual language.
  </acceptance_criteria>
  <done>The onboarding journey is real end to end, gated by a truthful flag.</done>
</task>

<task type="auto">
  <name>Task 6: Full gates, live responsive proof, tracking updates</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, frontend/README.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (UI-01, UI-04 rows); .planning/ROADMAP.md Phase 9.2 (done-when);
      .planning/STATE.md formats
  </read_first>
  <action>
    1. Frontend gates in order: lint, typecheck, full test:run, build (dist/ produced). Backend:
       docker compose exec -T web python -m pytest -q (788 + new tests, all green).
    2. Live responsive proof in the browser (the done-when: "Layout adapts across breakpoints with
       the disclaimer present"): with the stack up and npm run dev running, use the preview browser at
       desktop (≥1280px) and mobile (≤400px) viewports on /dashboard: assert 3-col vs top-bar+tab-bar
       chrome visually and via DOM (sidebar present vs tab bar present), and the footer disclaimer
       string present at both widths. Capture both observations in the summary.
    3. Live journey proof: register a fresh account through the UI (or reuse a seeded one), verify the
       unverified routing, then walk login → onboarding wizard (3 steps) → dashboard release. If email
       verification cannot complete in dev (no SMTP), verify the user's is_verified directly in the
       container for the journey test and note it (dev-only shortcut, documented).
    4. Tracking updates: REQUIREMENTS.md UI-01 and UI-04 rows evidenced (shells + auth/wizard shipped;
       note the dashboard-content boundary is 9.3's); ROADMAP.md tick 09-02 with date + scope line;
       STATE.md — current phase executed, D1–D4 added to the phase decisions line, counters, and any
       new findings recorded (expected: any field-name divergences between mockups/spec and the real
       serializers).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run test:run &amp;&amp; npm run build &amp;&amp; docker compose exec -T web sh -c "python -m pytest -q" &amp;&amp; git diff --quiet -- config/settings/</automated>
    <fails_when>Any gate fails; the backend suite regresses beyond the deliberate Task 1 change + its tests; settings changes left behind.</fails_when>
  </verify>
  <acceptance_criteria>
    - All gates green (frontend suite, build, backend suite including the new profile_completed
      tests); the live breakpoint + journey proofs observed and recorded.
    - REQUIREMENTS/ROADMAP/STATE reflect reality: UI-01/UI-04 evidenced, 09-02 ticked, decisions and
      findings recorded.
  </acceptance_criteria>
  <done>The phase is gated, proven live, and tracked honestly.</done>
</task>

</tasks>

<threat_model>
**T-09.2-01 (MEDIUM): The wizard gate locks out every user.** The gating redirect reads
profile_completed; if Task 1's computation is wrong (field-name mismatch, null-vs-missing confusion),
every signed-in user loops into /onboarding forever. Mitigation: Task 1's tests pin both directions
before the frontend consumes the flag; the gate exempts /onboarding itself. Verified by: the accounts
test module plus the wizard gating test.
**T-09.2-02 (MEDIUM): Enumeration/abuse via auth screens.** Forgot-password copy must stay
enumeration-safe ("If that email exists…"); register must not leak whether an email is taken beyond
what the API already returns. Mitigation: spec copy used verbatim; client-side validation is UX only,
server remains the authority. Verified by: RegisterPage/ForgotPassword tests asserting the exact
strips.
**T-09.2-03 (LOW): Staff chrome leaks.** Nav items or routes hinting at staff surfaces violate D2 and
could mislead. Mitigation: no staff links in SidebarNav/MobileTabBar; the two staff routes remain
routed but unlinked (server-side enforcement is the authority, 04 §113). Verified by: grep for staff
labels in the nav components + AppShell test.
</threat_model>
