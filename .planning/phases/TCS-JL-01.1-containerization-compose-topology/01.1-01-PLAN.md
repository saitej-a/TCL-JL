---
phase: 01.1-containerization-compose-topology
plan: 01.1-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .gitattributes
  - .env.example
  - Makefile
  - Dockerfile
  - docker/web_placeholder.py
  - docker/celery_placeholder.py
  - docker/beat_placeholder.py
  - docker-compose.yml
  - docker-compose.prod.yml
  - nginx/nginx.dev.conf
  - nginx/nginx.prod.conf
autonomous: true
requirements: []   # infrastructure sub-phase — no REQ-IDs mapped (phase_req_ids: null); owns spec tasks T1.1–T1.2
user_setup:
  - service: github-branch-protection
    why: "T1.1 requires main to require PR + passing checks — dashboard-only action"
    dashboard_config:
      - task: "Protect main branch"
        location: "GitHub repo → Settings → Branches → Add branch protection/ruleset"
        details: "Require pull request before merge; require status checks to pass (CI checks arrive in Phase 1.2)"
must_haves:
  truths:
    - "D-01: web runs the Python stdlib placeholder responder on 0.0.0.0:8000 — entrypoint swap point for 1.2 gunicorn"
    - "D-02: celery_worker and celery_beat run placeholder workloads — all six services boot in 1.1"
    - "D-03: nginx present in BOTH compose files — dev HTTP :80, prod TLS"
    - "D-04: prod nginx.conf declares TLS 1.3 + HSTS with gitignored placeholder certs"
    - "D-05: single celery_worker consuming -Q default,notifications,maintenance"
    - "D-06: identical worker shape in dev and prod"
    - "D-07: exact minor pins (postgres:16.15-alpine, redis:7.4.11-alpine, python:3.11.12-slim, nginx:1.27-alpine) — verified via docker manifest inspect"
    - "D-08: one shared Dockerfile serves web/celery_worker/celery_beat via command overrides"
    - "D-09: healthcheck-gated depends_on chain — first `docker compose up` boots clean deterministically"
    - "D-10: named volumes postgres_data + redis_data; dev bind-mounts source for hot-reload"
    - "D-11: dev uses env_file .env; prod uses environment mappings; .env gitignored"
    - "docker compose up -d boots all six services (web, db, redis, celery_worker, celery_beat, nginx) with no container exits"
    - "curl http://localhost/ (host nginx :80) returns HTTP 200 with the placeholder body"
    - "docker compose config --quiet passes for BOTH dev and prod compose files"
    - "All four pinned image tags resolve to real registry manifests"
    - "git status is clean of .env and nginx/certs/* (secrets ignored)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Dev topology: 6 services, healthcheck-gated depends_on, named volumes, bind mounts, env_file"
      contains: "service_healthy"
    - path: "docker-compose.prod.yml"
      provides: "Prod topology: TLS 1.3 nginx, environment-based config, no db/redis host ports"
      contains: "443 ssl"
    - path: "Dockerfile"
      provides: "Shared image for web/celery_worker/celery_beat from python:3.11.12-slim"
      contains: "python:3.11.12-slim"
    - path: "docker/web_placeholder.py"
      provides: "HTTP responder binding 0.0.0.0:8000"
      contains: "0.0.0.0"
    - path: "nginx/nginx.prod.conf"
      provides: "TLS 1.3 listener + HSTS + proxy to web:8000"
      contains: "TLSv1.3"
    - path: ".gitignore"
      provides: "T1.1 exclusions + nginx/certs/"
      contains: "serviceAccountKey.json"
  key_links:
    - from: "nginx/nginx.dev.conf"
      to: "web:8000"
      via: "proxy_pass to web service on the compose network"
      pattern: "proxy_pass http://web:8000"
    - from: "docker-compose.yml"
      to: "db/redis healthchecks"
      via: "depends_on condition: service_healthy on web and celery_worker"
      pattern: "condition: service_healthy"
---

<objective>
Establish repository hygiene and the full six-service Docker Compose topology so the first `docker compose up` boots web, db (PostgreSQL 16.15), redis (7.4.11), celery_worker, celery_beat, and nginx cleanly with placeholder workloads.

Purpose: Phase 1 success criterion 1 — deterministic containerized foundation every later phase builds on. Placeholder entrypoints (web/celery) are swapped for real Django in Phase 1.2 without touching service definitions.

Output: docker-compose.yml, docker-compose.prod.yml, Dockerfile, placeholder scripts, nginx dev/prod configs, .gitignore/.gitattributes/.env.example/Makefile, self-signed placeholder certs (gitignored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/TCS-JL-01.1-containerization-compose-topology/01.1-CONTEXT.md
@.planning/phases/TCS-JL-01.1-containerization-compose-topology/01.1-RESEARCH.md
</context>

<threat_model>
<!-- ASVS L1 (config: security_enforcement=true, block_on=high) — infrastructure attack surface -->
<asset name="Environment secrets" value="POSTGRES_PASSWORD and future FCM/email credentials flow through .env / environment">
  <threat id="T-1.1-01" severity="high" description="Secrets committed to git (misplaced .env, cert private keys)">Mitigation: .gitignore excludes .env, *.pem, *.key, nginx/certs/ (T1.1); only .env.example tracked; verification includes git check-ignore + git status clean check. Residual: none — block-on-high mitigated at source.</threat>
  <threat id="T-1.1-02" severity="medium" description="Placeholder self-signed TLS mistaken for production-grade">Mitigation: certs generated into gitignored nginx/certs/; nginx.prod.conf carries a comment banner; Phase 10.2 owns real certs + hardening.</threat>
  <threat id="T-1.1-03" severity="high" description="DB/Redis exposed to host network in prod compose">Mitigation: docker-compose.prod.yml defines NO ports: for db/redis (network-internal only); dev binds these to 127.0.0.1:5432/127.0.0.1:6379 or omits them; nginx is the single ingress. Block-on-high satisfied: prod file must not contain 5432:/6379: host mappings.</threat>
  <threat id="T-1.1-04" severity="low" description="Nginx proxy hides client identity from Django">Mitigation: both nginx confs set X-Forwarded-For/X-Forwarded--Proto headers — Django USE_X_FORWARDED_* wiring in 1.2 consumes them.</threat>
</asset>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Repository hygiene — gitignore, gitattributes, env template, Makefile</name>
  <files>.gitignore, .gitattributes, .env.example, Makefile</files>
  <read_first>10_MVP_TASKS.md (T1.1 spec lines 87-88), 01.1-CONTEXT.md (D-11, Claude's Discretion)</read_first>
  <action>Create .gitignore with exactly: .env, *.pem, serviceAccountKey.json, media/, node_modules/ (T1.1 list), plus nginx/certs/ (cert placeholders), __pycache__/, *.pyc, .venv/, db.sqlite3. Create .gitattributes forcing LF for container-consumed text on this Windows host: '*.sh text eol=lf', '*.conf text eol=lf', '*.yml text eol=lf', '*.yaml text eol=lf', '*.py text eol=lf', '*.env* text eol=lf' — CRLF in mounted confs breaks nginx (RESEARCH pitfall 2). Create .env.example (TRACKED) containing: COMPOSE_PROJECT_NAME=tcs_jl, POSTGRES_USER=tcs_admin, POSTGRES_PASSWORD=change-me-dev-only, POSTGRES_DB=tcs_joining_tracker, TZ=Asia/Kolkata, REDIS_URL=redis://redis:6379/0, CELERY_BROKER_URL=redis://redis:6379/1, CELERY_RESULT_BACKEND=redis://redis:6379/2 — matching the T1.5 redis db partitioning (0=cache/throttle, 1=broker, 2=results). Create Makefile with targets: up (docker compose up -d --build), down (docker compose down), logs (docker compose logs -f --tail=100), ps (docker compose ps), rebuild (docker compose build --no-cache), prod-config (docker compose -f docker-compose.prod.yml config --quiet) — space-form `docker compose`, no version key anywhere.</action>
  <verify>git check-ignore .env nginx/certs/server.key serviceAccountKey.json exits 0; .env.example is tracked (git ls-files shows it); file .gitattributes contains 'eol=lf'</verify>
  <acceptance_criteria>
    - .gitignore contains '.env', 'serviceAccountKey.json', 'media/', 'node_modules/', '*.pem', 'nginx/certs/'
    - .gitattributes contains '*.conf text eol=lf' and '*.yml text eol=lf'
    - .env.example contains 'POSTGRES_PASSWORD=' and 'CELERY_BROKER_URL=redis://redis:6379/1'
    - Makefile contains 'up:' target invoking 'docker compose up -d'
  </acceptance_criteria>
  <done>All four hygiene files exist with the exact content specified; secrets patterns ignored, template tracked</done>
</task>

<task type="auto">
  <name>Task 2: Pin verification, shared Dockerfile, placeholder workloads</name>
  <files>Dockerfile, docker/web_placeholder.py, docker/celery_placeholder.py, docker/beat_placeholder.py</files>
  <read_first>01.1-RESEARCH.md (Standard Stack pins, Open Question 1 fallbacks), 01.1-CONTEXT.md (D-01, D-02, D-07, D-08)</read_first>
  <action>First verify pins resolve: run `docker manifest inspect postgres:16.15-alpine`, `docker manifest inspect redis:7.4.11-alpine`, `docker manifest inspect python:3.11.12-slim`, `docker manifest inspect nginx:1.27-alpine`. If an -alpine variant does not exist for postgres/redis, fall back to the non-alpine tag (postgres:16.15, redis:7.4.11) and record the substitution in the plan SUMMARY. Create Dockerfile: FROM python:3.11.12-slim, WORKDIR /app, ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1, COPY docker/ /app/docker/, default CMD ["python", "/app/docker/web_placeholder.py"] — no pip installs yet (no requirements until 1.2). Create docker/web_placeholder.py: stdlib http.server on 0.0.0.0:8000 (binding 0.0.0.0 is REQUIRED — 127.0.0.1 is unreachable from nginx/host through the bridge network), responding 200 with body 'TCS Joining Tracker - placeholder web (Phase 1.1)' on any path. Create docker/celery_placeholder.py: infinite loop printing '[celery_worker placeholder] queues default,notifications,maintenance' every 30s. Create docker/beat_placeholder.py: infinite loop printing '[celery_beat placeholder] scheduler tick' every 60s. All three use PYTHONUNBUFFERED-friendly prints and handle KeyboardInterrupt cleanly.</action>
  <verify>docker manifest inspect exits 0 for each pinned tag (or documented fallback used); python -c "import ast; ast.parse(open('docker/web_placeholder.py').read())" passes for all three scripts</verify>
  <acceptance_criteria>
    - Dockerfile contains 'FROM python:3.11.12-slim' (or verified substitute)
    - docker/web_placeholder.py contains '0.0.0.0' and '8000' and NOT '127.0.0.1'
    - docker/celery_placeholder.py contains 'default,notifications,maintenance'
    - All three placeholder scripts parse as valid Python
  </acceptance_criteria>
  <done>Pins verified against registry; shared Dockerfile + three placeholder scripts exist and parse</done>
</task>

<task type="auto">
  <name>Task 3: Compose topology, nginx configs, placeholder certs, deterministic boot verification</name>
  <files>docker-compose.yml, docker-compose.prod.yml, nginx/nginx.dev.conf, nginx/nginx.prod.conf, nginx/certs/ (generated, gitignored)</files>
  <read_first>01.1-CONTEXT.md (D-01..D-11), 01.1-RESEARCH.md (Patterns 1-2, Pitfalls 1-3, TLS example), 10_MVP_TASKS.md (T1.2 service list)</read_first>
  <action>Build docker-compose.yml (dev, NO top-level version: key) with six services per T1.2: web (build ., command placeholder, env_file .env, volumes ['.:/app'], depends_on db+redis condition service_healthy, NOT exposed to host — nginx owns ingress); db (image postgres:16.15-alpine per verified pin, env_file .env, volume postgres_data:/var/lib/postgresql/data, healthcheck CMD-SHELL 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}' interval 5s timeout 3s retries 10 start_period 15s); redis (image redis:7.4.11-alpine per verified pin, volume redis_data:/data, healthcheck CMD 'redis-cli ping' same cadence); celery_worker (build ., command python /app/docker/celery_placeholder.py, volumes ['.:/app'], depends_on db+redis service_healthy); celery_beat (build ., command python /app/docker/beat_placeholder.py, volumes ['.:/app'], same depends_on); nginx (image nginx:1.27-alpine, ports '80:80', volume ./nginx/nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro, depends_on web condition service_started). Top-level volumes: postgres_data, redis_data. Dev convenience: db ports '127.0.0.1:5432:5432' and redis ports '127.0.0.1:6379:6379' (loopback-only — threat T-1.1-03 does not apply to dev). Build docker-compose.prod.yml as an override-style sibling: same six services WITHOUT bind mounts, WITHOUT db/redis host ports at all, web command unchanged (placeholder until 1.2), nginx mounting ./nginx/nginx.prod.conf and ./nginx/certs:/etc/nginx/certs:ro, port '443:80' style TLS termination inside nginx, environment: mappings referencing ${POSTGRES_PASSWORD} etc. instead of env_file (RESEARCH pitfall 3). Create nginx/nginx.dev.conf: upstream web:8000, server listen 80, location / proxy_pass http://web:8000 with X-Forwarded-For and X-Forwarded-Proto headers. Create nginx/nginx.prod.conf: listen 443 ssl; http2 on; ssl_protocols TLSv1.3; ssl_certificate /etc/nginx/certs/server.crt; ssl_certificate_key /etc/nginx/certs/server.key; add_header Strict-Transport-Security 'max-age=31536000' always; proxy_pass http://web:8000 with forwarded headers; comment banner: 'PLACEHOLDER self-signed certs — Phase 10.2 hardens'. Generate placeholder certs: openssl req -x509 -newkey rsa:2048 -keyout nginx/certs/server.key -out nginx/certs/server.crt -days 30 -nodes -subj '/CN=localhost' (dir exists via task 1 gitignore; files stay untracked). Verify end-to-end: `docker compose -f docker-compose.yml config --quiet` AND `docker compose -f docker-compose.prod.yml config --quiet` (prod needs env vars present — use `POSTGRES_PASSWORD=x docker compose -f docker-compose.prod.yml config --quiet` or a throwaway env); then `docker compose up -d --build`, wait for health, `docker compose ps` shows 6 services with web/db/redis healthy and zero exited containers, `curl -fsS http://localhost/` returns the placeholder body, `docker compose down` cleans up.</action>
  <verify>Both compose files pass config --quiet; docker compose ps shows 6 running/healthy services, none exited; curl -fsS http://localhost/ returns 200 with placeholder body; git status does not list nginx/certs files</verify>
  <acceptance_criteria>
    - docker-compose.yml contains 'service_healthy' at least 4 times (web, celery_worker, celery_beat) and 'pg_isready' and 'redis-cli ping'
    - docker-compose.yml does NOT contain a top-level 'version:' key
    - docker-compose.prod.yml does NOT contain host port mappings for 5432 or 6379 (threat T-1.1-03)
    - nginx/nginx.prod.conf contains 'TLSv1.3' and 'Strict-Transport-Security' and 'proxy_pass http://web:8000'
    - nginx/nginx.dev.conf contains 'proxy_pass http://web:8000' and 'X-Forwarded-For'
    - `curl -fsS http://localhost/` returns HTTP 200 containing 'placeholder web (Phase 1.1)'
    - `git check-ignore nginx/certs/server.key` exits 0
  </acceptance_criteria>
  <done>Six-service topology boots deterministically on first `docker compose up`; nginx dev proxy serves the placeholder; prod file validates with TLS 1.3 and no exposed data stores</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose -f docker-compose.yml config --quiet` exits 0
- [ ] `docker compose -f docker-compose.prod.yml config --quiet` exits 0 (with env vars provided)
- [ ] `docker compose up -d --build` → `docker compose ps`: 6 services, 0 exited, web/db/redis healthy
- [ ] `curl -fsS http://localhost/` returns 200 placeholder body
- [ ] `docker compose down` leaves no orphan containers
- [ ] `git check-ignore .env nginx/certs/server.key` exits 0 and `git status` clean of secrets
</verification>

<success_criteria>

- All tasks completed with acceptance criteria met
- Phase 1 success criterion 1 satisfied: `docker compose up` boots Django-stack topology (placeholder web) without container exits
- All CONTEXT.md decisions D-01..D-11 reflected in the files above

</success_criteria>

<output>
After completion, create `.planning/phases/TCS-JL-01.1-containerization-compose-topology/01-01-SUMMARY.md`
</output>
